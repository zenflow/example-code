{
  "scripts": {
    "_copy-vendor": "cpy ../packages/example-code-dom/dist/example-code-dom.umd.js ./static/vendor",
    "start": "npm run _copy-vendor && docusaurus-start",
    "build": "npm run _copy-vendor && docusaurus-build",
    "publish": "npm run _copy-vendor && docusaurus-publish",
    "write-translations": "docusaurus-write-translations",
    "version": "docusaurus-version",
    "rename-version": "docusaurus-rename-version"
  },
  "devDependencies": {
    "cpy-cli": "^1.0.1",
    "docusaurus": "^1.0.9",
    "gh-pages": "^1.1.0"
  }
}
